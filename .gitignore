# OS files
.DS_Store

# Bytecode cache
__pycache__/
*.pyc
*.py[cod]
*.pyo

# Node dependencies — scheduler uses Node but `node_modules` is huge
# and reproducible via `npm install` (or the lockfile in scheduler/).
node_modules/

# Editor / backup files
*.bak
*.swp
# Virtual environments
.env
.env.*
!.env.example
.venv/
.venv_*/
venv/

# Run outputs
runs/
fastloop_*_20*/
observe_*.log
*.log
candidate_journal.jsonl
oracle_open_prices.json
oracle_price_samples.json
candidate_replay_report*.txt
entered_markets.json
market_quality_state.json
replay.json
daily_spend.json
